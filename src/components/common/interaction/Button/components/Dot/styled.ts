import { Animated } from 'react-native';
import styled from 'styled-components/native';

export const DotContainer = styled(Animated.View)`
  width: 6px;
  height: 6px;
  border-radius: 6px;
  margin-right: 3px;
  background-color: #FFFFFF;
`;
